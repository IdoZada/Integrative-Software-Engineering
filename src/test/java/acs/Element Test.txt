Test  createNewElement
	
	GIVEN: 
		the server is up
	
	WHEN:
		invoke POST /acs/elements/2020b.demo/demo@gmail.com
	
	THEN:
		status 2XX and JSON
	
	RETURN:
		status 200 OK
		-----------------------------------------------------------------------------------------------------------------
	
	Test  createNewElement
	
	GIVEN: 
		the server is up
	
	WHEN:
		invoke POST /acs/elements//
	
	THEN:
		status 404
	
	RETURN:
		status 404 Not Found

=========================================================================================================

Test  updateElement
	
	GIVEN: 
		the server is up
	
	WHEN:
		invoke PUT /acs/elements/manegerDemo/manager@gmail.com/elementDEmo/2020
		
	
	THEN:
		status 2XX and JSON
	
	RETURN:
		status 200 OK
		-----------------------------------------------------------------------------------------------------------------------------------------------------------------------
	
	Test  updateElement
	
	GIVEN: 
		the server is up
	
	WHEN:
		invoke PUT /acs/elements/{managerDomain}/{managerEmail}/{elementDomain}/{elementId}
		at least one of the parameters is missing(ex: element id)
	
	THEN:
		status 404
	
	RETURN:
		status 404 Not Found
==========================================================================================================

Test  getElement
	
	GIVEN: 
		the server is up
	
	WHEN:
		invoke GET /acs/elements/userDomain/user@gmail.com/elementDEmo/2020
		
	THEN:
		status 2XX and JSON
	
	RETURN:
		status 200 OK
		
		--------------------------------------------------------------------------------------------------------------------------------------------------
	Test  getElement
	
	GIVEN: 
		the server is up
	
	WHEN:
		invoke GET /acs/elements/userDomain/user@gmail.com/elementDEmo/
		at least one of the parameters is missing(ex: element id)
		
	THEN:
		status 404
	
	RETURN:
		status 404 Not Found
===========================================================================================================
Test  getAllElements
	
	GIVEN: 
		the server is up
	
	WHEN:
		invoke GET /acs/elements/userDomain/user@gmail.com
		
	THEN:
		status 2XX and JSON
	
	RETURN:
		status 200 OK
		
		---------------------------------------------------------------------------------------------------------------------------
	Test  getAllElements
	
	GIVEN: 
		the server is up
	
	WHEN:
		invoke GET /acs/elements/userDomain/user@gmail.com
		at least one of the parameters is missing(ex: element id)
		
		
	THEN:
		status 404
	
	RETURN:
		status 404 Not Found
=============================================================================================================
		
		
